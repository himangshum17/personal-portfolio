import type { NextPage } from 'next';
import { Hero, PageContainer } from '../layouts';

const Home: NextPage = () => {
  return (
    <PageContainer>
      <Hero />
    </PageContainer>
  );
};

export default Home;
